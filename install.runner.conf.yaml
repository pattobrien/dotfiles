- defaults:
    link:
      relink: true
      create: true
    shell:
      stderr: true
      stdout: true
      stdin: true

- shell:
    - command: ./scripts/brew_bundle.zsh brew/runner/Brewfile
    - command: ./scripts/dequarantine.zsh
      description: Removing the quarantine flag from installed apps
