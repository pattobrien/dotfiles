- defaults:
    link:
      relink: true
      create: true
    shell:
      stderr: true
      stdout: true
      stdin: true

- shell:
    - command: brew bundle --file brew/personal/Brewfile
    - command: ./scripts/dequarantine.zsh
      description: Removing the quarantine flag from installed apps
