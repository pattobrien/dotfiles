- defaults:
        link:
              relink: true
              create: true
        shell:
              stderr: true
              stdout: true
              stdin: true

- shell:
        - command: brew bundle --file brew/personal/Brewfile
