/**
 * Types for the vendored xterm.js master headless bundle. The public headless
 * API is unchanged from the released @xterm/headless 6.0.0 (master adds the
 * kitty keyboard protocol and APC parser hooks internally), so the released
 * typings cover it.
 */
export { Terminal } from "@xterm/headless";
