- defaults:
    shell:
      stderr: true
      stdout: true
      stdin: true

- shell:
    - command: mise trust ~/.config/mise/config.toml
      description: Trusting mise config
    - command: mise install
      description: Installing tools from mise config
