#!/bin/sh
# Clear tmux pane screen and scrollback (used by kitty Cmd+K mapping)
# Silently no-ops when not in a tmux session.
TMUX_BIN=/opt/homebrew/bin/tmux
$TMUX_BIN send-keys -R C-l 2>/dev/null && sleep 0.05 && $TMUX_BIN clear-history 2>/dev/null
exit 0
