# Roadmap

## Features

- [x] native picker UI
- return result back to parent process
- custom filter command (e.g. `fzf`)

- use Picker with `wt attach <tab>`

### Stretch Features

- customizable UI style
