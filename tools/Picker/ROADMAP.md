# Roadmap

## Features

- [x] native picker UI
- return result back to parent process
- custom filter command (e.g. `fzf`)
- show `fzf` char highlights on each remaining item
- use Picker with `wt attach <tab>`
- back-to-back input fields
- different input field types

### Stretch Features

- customizable UI style
