{
  "name": "dotfiles",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:wt": "vp run wt-cli#dev",
    "dev:raycast": "vp run wt-raycast#dev",
    "ready": "vp fmt && vp lint && vp run test -r && vp run build -r",
    "lint": "vp lint",
    "lint:fix": "vp lint --fix",
    "format": "vp fmt --check",
    "format:fix": "vp fmt --write",
    "check": "vp check"
  },
  "devDependencies": {
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "typescript": "catalog:",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@11.9.0"
}
