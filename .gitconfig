[filter "lfs"]
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
[user]
	name = Patt's Mac Mini
	email = pattobrien@themachinemedia.com
[init]
	defaultBranch = master
