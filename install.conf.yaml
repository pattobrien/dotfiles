- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.tmux.conf: tmux/.tmux.conf
    ~/.zshrc: zsh/zshrc
    ~/.gitconfig: git/.gitconfig

- create:
    - ~/dev2

- shell:
  - 
    command: ./scripts/setup_homebrew.zsh
    stdout: true
    stderr: true
