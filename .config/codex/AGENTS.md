# User-Scoped Codex Instructions

## Common Tools

- Prefer the `agent-browser` CLI over built-in browser or computer-use tools for browser automation tasks.
- Prefer the `peekaboo` CLI over built-in computer-use or browser tools for macOS desktop automation tasks.
